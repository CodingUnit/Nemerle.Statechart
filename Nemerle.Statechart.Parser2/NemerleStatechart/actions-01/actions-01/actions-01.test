name enter_operand;
    
    event digit(char);
    entry / ent_op = prefix;
    exit / prefix = "";
    
    digit: => {
                n == '0' => zero;
                else    / insert(n) => int;
              }

    point: / insert('0') => frac;

    digit(n) / insert(n);
    point (=> frac;

    state zero
    {
      $> / insert('0');
      digit(i) [i == '0'] / ();
      digit(n) [else] / insert(n) => int;
    }

    state int
    {
    }

    state frac
    {
      entry / insert(',');
      point / ();
    }
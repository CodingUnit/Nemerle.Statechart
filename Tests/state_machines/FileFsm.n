using Nemerle.Collections;
using Nemerle.Text;
using Nemerle.Utility;

using System;
using System.Collections.Generic;
using System.Linq;
using Nemerle.Statechart;

namespace Nemerle.Statechart.Tests
{  

  [statechart(<#

  flags : auto_initial transition_completed_events;
  //flags : debug all_sync;

  //sync form;
  
  entry / SaveDis;

  NewFileCreated   => NotSaved;
  FileSaved        => NotNeedSaving;
  Exiting          => X;
  
  0 => Waiting;
  
  state NotNeedSaving
  {
    state Saved
    {
        
        entry / CaptionFileName SaveAsEna;
        Change => Changed;
    }
    
    state Waiting
    {
      entry / SaveAllDis CaptionProgName;
    }
  
  }
  
  state NotSaved
  {
      entry / SaveEna;

      state New
      {
          entry / CaptionFile SaveAsEna;
      }

      state Changed
      {
          entry / CaptionChanged;
      }

  }

  
  #>
  )]
  public class FileFsm
  {
    
  }

}

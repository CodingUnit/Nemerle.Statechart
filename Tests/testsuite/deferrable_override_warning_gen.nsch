

  flags testing;
  //flags debug;

  name "deferable event override error";

  state A
  {
    a / defer; // W: Transition "DeferrableEventaAtA" is overriden by \[TransitionBToCBya, TransitionCToBBya\], due to priority rules, because these transitions are always enabled

    state B
    {
      a => C;
    }

    state C
    {
      a => B;
    }
  }

  
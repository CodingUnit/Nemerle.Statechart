using Nemerle;
using Nemerle.Collections;
using Nemerle.Text;
using Nemerle.Utility;

using System;
using Nemerle.Statechart;

  /// <summary>
  /// Description of PathCover.
  /// </summary>
  [statechart(<#

  flags testing;

  a [a1] => C;

  state A
  {
    a       => B;
  }

  state B
  {
  }

  state C
  {
  }
  #>
  )]
  public class TestFsm
  {
    public a1 : bool {get;set;default true}
  }


module Test
{
  Main() : void
  {
    def fsm = TestFsm();
    def tester = fsm.GetTester();
    tester.ConsoleTestInit();

      // "Starting state machine"
    tester.Initiate(["A"]);

    tester.Test("a", "B", "ExitA");
    tester.Test("a", "C", "ExitB", "ExitTop", "EntryTop");
    fsm.Terminate()
  }
}

/*
BEGIN-OUTPUT
EntryA
Ok
Processing a 'a'
ExitA
EntryB
Ok
Processing a 'a'
ExitB
ExitTop
EntryTop
EntryC
Ok
END-OUTPUT
*/
